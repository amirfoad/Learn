@model IEnumerable<Role>
@{ 
    ViewData["Title"] = "لیست نقش ها";
}

<div class="headline buttons two primary">
    <h4> لیست نقش ها</h4>

</div>
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                لیست نقش ها ی کاربران
            </div>
            <!-- /.panel-heading -->
            <div class="panel-body">
                <div class="table-responsive">
                    <div id="dataTables-example_wrapper" class="dataTables_wrapper form-inline" role="grid">
                        @*<div class="row">
                                <form>
                                    <div class="col-sm-5 col-md-5">
                                        <input type="text" name="filterUserName" class="form-control" placeholder="نام کاربری" />
                                    </div>
                                    <div class="col-sm-5 col-md-5">
                                        <input type="text" name="filterEmail" class="form-control" placeholder="ایمیل" />
                                    </div>
                                    <div class="col-sm-2 col-md-2">
                                        <button type="submit" class="btn btn-info">بگرد</button>
                                        <a class="btn btn-default btn-sm" href="/Admin/Users">خالی</a>
                                    </div>
                                </form>
                            </div>*@
                        <div class="col-md-12" style="margin: 10px 0;">

                            <a class="btn btn-outline btn-success" href="/admin/user/CreateRole">افزودن نقش جدید</a>

                        </div>
                        <table class="table table-striped table-bordered table-hover dataTable no-footer" id="dataTables-example" aria-describedby="dataTables-example_info">
                            <thead>
                                <tr>
                                    <th>عنوان نقش</th>

                                    <th>دستورات</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.RoleTitle</td>



                                        <td>
                                            <a href="/Admin/user/EditRole/@item.RoleId" class="btn btn-warning btn-sm">
                                                ویرایش
                                            </a>
                                            <a href="/Admin/user/DeleteRole/@item.RoleId" class="btn btn-danger btn-sm">
                                                حذف
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>

                    </div>
                </div>

            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>