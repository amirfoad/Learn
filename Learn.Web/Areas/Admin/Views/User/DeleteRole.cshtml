@model Role
@{
    ViewData["Title"] = "حذف نقش";
}

<div class="container alert alert-danger">
    <form method="post">
        <input type="hidden" asp-for="RoleId" />
        <input type="hidden" asp-for="RoleTitle" />
        <h2>آیا از حذف نقش @Model.RoleTitle مطمئن هستید?</h2>
        <p>
            <input type="submit" value="حذف" class="btn btn-outline btn-danger" />
            <a class="btn btn-outline btn-info " href="/Admin/user/rolesIndex">انصراف</a>
        </p>
    </form>
</div>